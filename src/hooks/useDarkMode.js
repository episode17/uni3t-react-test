/**
 * TASK 05: Implement the dark mode hook
 * -------
 *
 * Create a hook that allows the user to switch between the light and dark
 * themes.
 *
 * The value must be stored and restored from local storage
 *
 * Hint:
 *  The hook returns an object like:
 *  { enabled, toggle }
 *
 * Hint:
 *  See `src/ThemeProvider` for a usage example
 */

export default function useDarkMode() {
  // ...

  return {};
}
